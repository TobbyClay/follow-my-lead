{
  "name": "follow-my-lead",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Learn reusable Chrome workflows from demonstrations, with a subscription-backed skill.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "demo": "node scripts/demo-server.mjs",
    "preview": "node scripts/preview-server.mjs",
    "validate": "node scripts/fml.mjs validate",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
